source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "jekyll-remote-theme"   # needed so Cloudflare & local builds can fetch the theme

# Only for local `jekyll serve` on Ruby 3+
gem "webrick", "~> 1.8"
